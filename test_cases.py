import unittest

import numpy as np
import quaternion
import pipeline
import pipeline as pl
import helper

class PipelineTestCase(unittest.TestCase):
    pass
    # def test_get_qref_w(self):
    #     x = [1, 0, 0]
    #     y = [0, 1, 0]
    #
    #     self.assertEqual(pl.Correction.get_qref_w(x, y), 1)

    # def test_get_sensor_diff(self):
    #     self.assertEqual(helper.get_sensor_diff(""), 5)
